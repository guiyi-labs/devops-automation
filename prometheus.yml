global:
  scrape_interval: 15s
  evaluation_interval: 30s
rule_files:
  - /etc/prometheus/prometheus-alerts.yml
scrape_configs:
  - job_name: easyops-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']